apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: velflix-fpm
  name: velflix-fpm
  namespace: velflix-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: velflix-fpm
  strategy: {}
  template:
    metadata:
      labels:
        app: velflix-fpm
    spec:
      containers:
      - image: mahdi0188/velflix-fpm:dev-61-b8b8adc
        name: velflix-fpm
        resources: {}
        envFrom:
          - secretRef:
              name: app-env-secrets
status: {}
